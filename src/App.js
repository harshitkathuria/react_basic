import './App.css';

// App component (can be a class)
function App() {
  return (
    // JSX - Javascript Syntax Extension (similar to HTML)
    // replace class with className
    // JSX must return a single parent element
    <div className="App">
      <h1>First React Application </h1>
      <h2>By Harshit Kathuria</h2>
    </div>
  );
}

export default App;
